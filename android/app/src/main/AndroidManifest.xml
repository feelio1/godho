<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- 위치 권한은 '주변 병원' 탭 최초 진입 시에만 요청합니다 (앱 시작 시 강제 요청 금지). -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <!-- 카카오맵 SDK 요구사항 (OpenGL ES 2.0 이상). -->
    <uses-feature android:glEsVersion="0x00020000" android:required="true" />
    <!-- 진료 예약 알림(Android 13+). 예약에 알림을 처음 추가하는 시점에만
         런타임으로 요청한다 — 앱 시작 시 강제 요청 금지(nearby_map과 같은
         원칙). 거부되어도 앱은 정상 동작하고 알림만 비활성화된다. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- 예약 알림을 지정한 시각에 정확히 울리기 위한 권한(Android 12+).
         USE_EXACT_ALARM(자동 부여)은 알람·캘린더류 앱 전용으로 스토어 정책상
         제한이 있어, 사용자가 직접 허용하는 SCHEDULE_EXACT_ALARM을 쓴다 —
         알림을 처음 설정할 때 NotificationService가 안내·요청하고, 거부되면
         근사 시각(inexact) 알림으로 자동 대체된다(스프린트 12 지시서 1). -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <application
        android:label="petcliniccheck"
        android:name=".MainApplication"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
        <!-- AdMob App ID. Value is injected by app/build.gradle.kts from the
             gitignored android/secrets.properties (falls back to Google's
             public test App ID when that file/key is absent — see
             android/secrets.properties.example). -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="${admobAppId}" />
        <!-- google_mobile_ads pulls in androidx.work (WorkManager) for its own
             internal background telemetry. Its default androidx.startup-based
             auto-initializer was crashing the whole process at cold start
             (java.lang.RuntimeException: Failed to create an instance of
             androidx.work.impl.WorkDatabase, thrown from
             androidx.work.WorkManagerInitializer during
             InitializationProvider.onCreate — i.e. before any Flutter/Dart
             code runs, so this cannot be fixed from the Dart side). We opt
             out of that automatic initialization here and initialize
             WorkManager ourselves instead, safely, in MainApplication. -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="tel" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="nmap" />
        </intent>
    </queries>
</manifest>
