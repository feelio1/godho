# 펫병원체크 (PetClinicCheck) — 프로젝트 규칙

## 이 앱이 무엇인가
동물병원 방문 전, 공개된 행정·가격 정보를 확인하는 **팩트체크 앱**. 병원을 평가·판정하지 않고, 보호자가 스스로 판단하도록 공공데이터 사실만 보여준다. 검색 중심(지도 아님) 확인 앱이다.

## 절대 원칙 (모든 코드·문구가 준수)
1. **평가 금지**: '좋은/나쁜/위험 병원', 신뢰도 점수, 위험 등급, '안전한 순' 정렬을 만들지 않는다.
2. **운영 기간 ≠ 신뢰도**: 오래됨을 좋음으로, 신규를 나쁨으로 치환하지 않는다. 신규는 "공개 데이터에서 확인할 수 있는 정보가 부족합니다"로 표기.
3. **원장 개인 추정 금지**: 주소·상호 유사성으로 동일 운영자를 암시하지 않는다.
4. **출처·날짜 필수**: 상세 화면에는 항상 최종 갱신일과 출처를 표기.
5. **'확인 안 됨'을 솔직히**: 데이터 없으면 추정·계산하지 않는다. 데이터 없음 ≠ 문제 있음을 UI에서 분리.

## 워딩 규칙 (하드코딩 문구는 이 표현을 쓸 것)
- "폐업 N회" (X) → "동일 주소에서 확인된 동물병원 인허가 기록 N건" (O)
- 병합 구간: "운영 N년 (기간 중 재등록 기록 M건 포함)" + 탭 시 원 기록 펼침. "휴업 후 재개" 등 서사 추정 금지.
- 진료비 비교: '비싸다/바가지' (X) → "지역 평균보다 높은 편 / 낮은 편 / 비슷함 / 비교할 데이터가 부족함" (O)
- 타임라인 하단 필수 고지: "이 정보는 동일한 주소에서 확인된 병원 인허가 기록입니다. 각 병원의 운영자가 동일한 사람인지는 확인할 수 없습니다."
- 색상: 빨강은 시스템 오류에만(스프린트 10부터 '폐업'도 회색 — 그 자리가 문제 있어 보이지 않게). 폐업·신규·정보부족은 모두 회색(중립).

## 기술 스택
- Flutter (Android 우선), Dart
- 상태관리: **Riverpod**
- 지도: 네이버 지도 SDK (flutter_naver_map) — '주변 병원' 탭 전용
- 로컬 저장: SharedPreferences (관심 병원 id 배열) — 계정·서버 없음
- 데이터: 정적 JSON 번들(assets/hospitals.json)을 앱 시작 시 로드, 메모리에서 검색/필터. Firestore 사용 안 함.

## 데이터 번들 스키마 (assets/hospitals.json)
- hospitals[]: id, name, status(open|closed|unknown), sido, sigungu, roadAddr, jibunAddr, lat, lng(null 가능), phone(null 가능), openDate, closeDate, continuousSince, merged, timelineId(null 가능)
- timelines{}: { addrKey, segments[]: { names[], start, end, status, merged } }
- 좌표 null인 병원은 지도 마커에서 제외, 검색·상세에는 정상 표시.

## 정보구조
- 하단 탭 4개: 홈(검색) / 주변 병원(지도) / 진료기록(반려동물 건강기록·예약) / 저장 (스프린트 9에서 진료기록을 홈 진입 카드에서 탭으로 승격)
- 비교는 탭 아님: 검색결과·상세에서 "비교 추가" → 하단 플로팅 바 → 비교 화면(최대 3곳)
- 설정·출처·이용안내는 홈 우측 상단 메뉴

## 하지 말 것
- 사용자 리뷰·제보·평점·커뮤니티 기능 (v1 제외)
- 병원에게 과금하는 어떤 구조 (상단노출·광고·예약수수료)
- 위치 권한을 앱 시작 시 강제 요청 ('주변 병원' 진입 시 요청)
- localStorage 등 브라우저 스토리지 (네이티브 앱임)
